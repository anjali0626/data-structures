// Implement Hash Table data structure

var HashTable = function() {
  // TODO...
};

// Store the passed in key-value pair in hash table
HashTable.prototype.set = function(key, value) {
  // TODO...
};

// Return the value associated with passed in key in hash table
HashTable.prototype.get = function(key) {
  // TODO...
};

// Delete value associated with the passed in key in hash table
HashTable.prototype.delete = function(key) {
  // TODO...
};

// Find if a value is associated with the passed in key in hash table
HashTable.prototype.has = function(key) {
  // TODO...
};

// Invoke passed in callback for each key-value pair in hash table
HashTable.prototype.forEach = function(callback) {
  // TODO...
};

// Find total number of key-value pairs in hash table
HashTable.prototype.count = function() {
  // TODO...
};